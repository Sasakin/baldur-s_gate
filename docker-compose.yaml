# docker-compose.yml
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - ./node_modules:/app/node_modules_external:ro
    environment:
      - NODE_ENV=development
    restart: always